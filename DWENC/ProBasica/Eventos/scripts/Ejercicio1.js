/*
    1. (Evento onLoad). Confeccionar una página que muestre un mensaje con
    la función alert inmediatamente después que se cargue.
*/

window.addEventListener('load', () => {
    alert('Cargado');
});