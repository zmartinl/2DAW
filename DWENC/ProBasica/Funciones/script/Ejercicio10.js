// Raiz cuadrada.

window.addEventListener('load', () => {
    let num = Number(prompt('Introduce numero'));
    let raiz = numero => {
        return Math.sqrt(numero);
    }

    alert(raiz(num));
});